# LDPlayer Visual UI Tester - Python 3.11+
#
# Core engine
numpy>=1.26
opencv-python>=4.9          # template matching, colour analysis, image maths
Pillow>=10.2                # image decoding for user-provided reference images
pyautogui>=0.9.54           # mouse and keyboard input
mss>=9.0                    # capture fallback (non-Windows / secondary path)

# OCR (default engine). Installing paddlepaddle can take a while; the app also
# works with Tesseract (pip install pytesseract) or with OCR disabled.
paddleocr>=2.7
paddlepaddle>=2.6

# Optional helpers
pyperclip>=1.8              # typing non-ASCII text through the clipboard

# Development
pytest>=8.0
